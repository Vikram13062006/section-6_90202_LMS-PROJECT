# Public API base URL for frontend build
# Local dev proxy target for Vite
VITE_BACKEND_URL=http://localhost:8080

# Optional direct API base URL for builds or when bypassing the proxy
# VITE_API_URL=http://localhost:8080/api
